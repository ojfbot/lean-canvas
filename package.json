{
  "name": "lean-canvas",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev:all": "pnpm --filter @lean-canvas/api dev & pnpm --filter @lean-canvas/browser-app dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
