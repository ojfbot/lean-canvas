import React from 'react'
import { Tile } from '@carbon/react'

// Placeholder — exposed as Module Federation remote for shell integration
export default function Settings() {
  return (
    <Tile>
      <h2>Lean Canvas Settings</h2>
      <p>API endpoint configuration and canvas preferences.</p>
    </Tile>
  )
}
